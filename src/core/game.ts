import readline from 'readline';
import PokemiltonMaster from '@/class/PokemiltonMaster'; // Replace 'your_classes_filename' with the actual filename
import Pokemilton from '@/class/Pokemilton';
import PokemiltonWorld from '@/class/PokemiltonWorld';
import fs from 'fs';

const rl = readline.createInterface({
	input: process.stdin,
	output: process.stdout,
});

function saveGameState() {}

function askForName() {}

function proposeFirstPokemilton(rl) {}

function startGame() {}

startGame();
